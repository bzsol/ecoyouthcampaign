on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: Use Node.js 18.17.1
      uses: actions/setup-node@v2
      with:
        node-version: '18.17.1'
      
    - name: 🔨 Build Project
      run: |
        npm install
        npm run build
    
    - name: List output files
      run: find dist/ -print
      
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.1.0
      with:
        server: ftp.ecoyouthcampaign.com
        username: ecoyouthcampaign@ftp.ecoyouthcampaign.com
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./dist/
        server-dir: /home/ecoyouthcampaign/public_html/
